@model IList<BTRole>
@{
    ViewBag.Title = "All Roles";
}

<h1>All Roles</h1>

@if (Model.Any())
{

}
else
{
    <div class="card">
        <div class="card-header">
            No Roles Found
        </div>
        <div class="card-body">
            <h5 class="card-title">
                Use the button below to create a role
            </h5>
            <a class="btn btn-primary" asp-controller="Admin" asp-action="AddRole">
                Add New Role
            </a>
        </div>
    </div>
}